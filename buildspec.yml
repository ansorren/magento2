version: 0.2

#env:
  #variables:
     # key: "value"
     # key: "value"
  #parameter-store:
     # key: "value"
     # key: "value"

phases:
  #install:
    #commands:
      # - command
      # - command
    #finally:
      # - command
      # - command
  #pre_build:
    #commands:
      # - command
      # - command
    #finally:
      # - command
      # - command
  build:
    commands:
      - alias composer="php /usr/local/bin/composer" && composer install --ignore-platform-reqs
      # - command
    #finally:
      # - command
      # - command
  #post_build:
    #commands:
      # - command
      # - command
    #finally:
      # - command
      # - command
artifacts:
  type: CODEPIPELINE
  packaging: zip
  files:
    - '**/*'
  base-directory: '*'
  discard-paths: yes
